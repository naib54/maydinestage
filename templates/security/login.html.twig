{% extends 'base_without_footer.html.twig' %}

{% block title %}
	{{'connexion' |trans }}
{% endblock %}

{% block body %}
	<form method="post">
		{% if error %}
			<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}

		{% if app.user %}
			<div class="mb-3">
				{{'connecté en tant que' |trans }}
				{{ app.user.userIdentifier }},
				<a href="{{ path('app_logout') }}">{{'deconnexion' |trans }}</a>
			</div>
		{% endif %}

		{# <h1 class="h3 mb-3 font-weight-normal">Se connecter</h1>
																												        <label for="username">Votre adresse Email</label>
																												        <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="email" required autofocus>
																												        <label for="password">Mot de passe</label>
																												        <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
																												
																												        <input type="hidden" name="_csrf_token"
																												               value="{{ csrf_token('authenticate') }}"
																												        >
																												
																												        {#
																												            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
																												            See https://symfony.com/doc/current/security/remember_me.html
																												
																												            <div class="checkbox mb-3">
																												                <label>
																												                    <input type="checkbox" name="_remember_me"> Remember me
																												                </label>
																												            </div>
																												        #}

		{# <button class="btn btn-lg btn-primary" type="submit">
																													Se connecter
																												</button>
																												# #}
		<div class="sidenav">
			<div class="login-main-text">
				<h2>May Dine France<br>
					{{'page de connexion' |trans }}</h2>
				<p>{{'connexion ou inscription' |trans }}</p>
			</div>
		</div>
		<div class="main">
			<div class="col-md-6 col-sm-12">
				<div class="login-form">
					<form>
						<div class="form-group">
							<label for="username">{{'votre adresse mail' |trans }}</label>
							<input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="email" required autofocus>
						</div>
						<div class="form-group">
							<label for="password">{{ 'mot de passe' |trans }}</label>
							<input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>

							<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
						</div>
						<button type="submit" class="btn btn-black">{{'connexion' | trans}}</button>
						<button type="submit" class="btn btn-secondary">{{'inscription' |trans }}</button>
					</form>
				</div>
			</div>
		</div>
	</form>
{% endblock %}
